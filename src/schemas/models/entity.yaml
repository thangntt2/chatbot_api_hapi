Entity:
  type: object
  properties:
    id:
      type: string
      format: uuid
    name:
      type: string
    type:
      type: string
      enum: ['trait', 'freetext', 'keyword']
    color:
      type: string